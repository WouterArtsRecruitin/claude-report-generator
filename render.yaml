services:
  - type: web
    name: claude-report-generator
    env: python
    plan: starter
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python claude_csv_report_generator.py
    envVars:
      - key: CLAUDE_API_KEY
        sync: false
      - key: PORT
        value: 5000